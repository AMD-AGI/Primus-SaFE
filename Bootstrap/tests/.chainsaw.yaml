apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: bootstrap-tests
spec:
  timeouts:
    apply: 10s
    assert: 10s
    exec: 15s
  cleanup:
    skipDelete: false
  execution:
    failFast: true
