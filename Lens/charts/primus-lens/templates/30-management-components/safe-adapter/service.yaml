{{- if and (eq (include "primus-lens.management.enabled" .) "true") .Values.management.safeAdapter.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "primus-lens.fullname" . }}-safe-adapter
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "primus-lens.labels" . | nindent 4 }}
    app.kubernetes.io/component: safe-adapter
spec:
  type: {{ .Values.management.safeAdapter.service.type }}
  ports:
  - port: {{ .Values.management.safeAdapter.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "primus-lens.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: safe-adapter
{{- end }}

