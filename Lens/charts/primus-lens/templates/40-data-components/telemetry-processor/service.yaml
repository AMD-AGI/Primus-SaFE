{{- if and (eq (include "primus-lens.data.enabled" .) "true") .Values.data.telemetryProcessor.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "primus-lens.fullname" . }}-telemetry-processor-data
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "primus-lens.labels" . | nindent 4 }}
    app.kubernetes.io/component: telemetry-processor-data
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.data.telemetryProcessor.service.httpPort }}
    targetPort: http
    protocol: TCP
    name: http
  - port: {{ .Values.data.telemetryProcessor.service.prometheusPort }}
    targetPort: prometheus
    protocol: TCP
    name: prometheus
  selector:
    {{- include "primus-lens.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: telemetry-processor-data
{{- end }}

