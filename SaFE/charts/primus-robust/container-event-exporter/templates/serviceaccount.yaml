{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "container-event-exporter.serviceAccountName" . }}
  labels:
    {{- include "container-event-exporter.labels" . | nindent 4 }}
{{- end }}
