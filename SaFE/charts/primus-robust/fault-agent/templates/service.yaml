apiVersion: v1
kind: Service
metadata:
  name: {{ include "fault-agent.fullname" . }}
  labels:
    {{- include "fault-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "fault-agent.selectorLabels" . | nindent 4 }}

