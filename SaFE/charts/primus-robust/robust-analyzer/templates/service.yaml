apiVersion: v1
kind: Service
metadata:
  name: {{ include "robust-analyzer.fullname" . }}
  labels:
    {{- include "robust-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "robust-analyzer.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
