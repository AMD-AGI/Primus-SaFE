#
# Copyright (C) 2025-2025, Advanced Micro Devices, Inc. All rights reserved.
# See LICENSE for license information.
#

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: faults.amd.com
spec:
  group: amd.com
  names:
    kind: Fault
    listKind: FaultList
    plural: faults
    singular: fault
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              action:
                description: Handling actions for the fault. e.g. reboot,taint
                type: string
              isAutoRepairEnabled:
                description: Whether the fault is auto repaired or not. default true
                type: boolean
              message:
                description: Fault message
                type: string
              monitorId:
                description: The id used by NodeAgent for monitoring.
                type: string
              node:
                description: Node information related to the fault
                properties:
                  adminName:
                    description: Fault-associated admin node name
                    type: string
                  clusterName:
                    description: The cluster which fault belongs to
                    type: string
                  k8sName:
                    description: Fault-associated k8s node name
                    type: string
                required:
                - adminName
                - clusterName
                - k8sName
                type: object
            required:
            - monitorId
            type: object
          status:
            properties:
              phase:
                description: The status of fault, e.g. Succeeded, Failed
                type: string
              updateTime:
                description: The last update time of fault
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
