#
# Copyright (C) 2025-2025, Advanced Micro Devices, Inc. All rights reserved.
# See LICENSE for license information.
#

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: inferences.amd.com
spec:
  group: amd.com
  names:
    kind: Inference
    listKind: InferenceList
    plural: inferences
    singular: inference
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .spec.modelForm
      name: ModelForm
      type: string
    - jsonPath: .spec.modelName
      name: ModelName
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Inference is the Schema for the inferences API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceSpec defines the desired state of Inference
            properties:
              config:
                description: Config contains additional configuration for different
                  phases
                properties:
                  entryPoint:
                    description: EntryPoint is the execution script
                    type: string
                  image:
                    description: Image is the container image
                    type: string
                  modelPath:
                    description: ModelPath is the model path
                    type: string
                type: object
              description:
                description: Description is the description of the inference service
                type: string
              displayName:
                description: DisplayName is the user-defined name for the inference
                  service
                type: string
              instance:
                description: Instance contains the inference instance information
                properties:
                  apiKey:
                    description: ApiKey is the inference service API key
                    type: string
                  baseUrl:
                    description: BaseUrl is the inference service URL
                    type: string
                  contextLength:
                    description: ContextLength is the context length
                    type: integer
                  workloadID:
                    description: WorkloadID is the workload ID
                    type: string
                type: object
              modelForm:
                description: ModelForm specifies the source of the model (API or ModelSquare)
                type: string
              modelName:
                description: ModelName is the name of the model
                type: string
              resource:
                description: Resource contains the resource requirements for the inference
                  service
                properties:
                  cpu:
                    description: Cpu is the CPU count
                    type: integer
                  gpu:
                    description: Gpu is the GPU count
                    type: string
                  memory:
                    description: Memory is the memory size in GB
                    type: integer
                  replica:
                    description: Replica is the number of replicas
                    type: integer
                  workspace:
                    description: Workspace is the workspace ID
                    type: string
                required:
                - workspace
                type: object
              userID:
                description: UserID is the ID of the user who owns this inference
                  service
                type: string
              userName:
                description: UserName is the name of the user
                type: string
            required:
            - displayName
            - instance
            - modelForm
            - modelName
            - resource
            - userID
            type: object
          status:
            description: InferenceStatus defines the observed state of Inference
            properties:
              events:
                description: Events records events for each phase
                items:
                  description: InferenceEvent records an event in the inference lifecycle
                  properties:
                    message:
                      description: Message contains additional event information
                      type: string
                    timestamp:
                      description: Timestamp is when this event occurred
                      format: date-time
                      type: string
                    workloadID:
                      description: WorkloadID is the associated workload ID
                      type: string
                    workloadPhase:
                      description: WorkloadPhase is the workload status
                      type: string
                  type: object
                type: array
              message:
                description: Message contains additional status information
                type: string
              phase:
                description: Phase is the current phase of the inference service
                type: string
              updateTime:
                description: UpdateTime is the last update time
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
