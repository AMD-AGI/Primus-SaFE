#
# Copyright (C) 2025-2025, Advanced Micro Devices, Inc. All rights reserved.
# See LICENSE for license information.
#

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: opsjobs.amd.com
spec:
  group: amd.com
  names:
    kind: OpsJob
    listKind: OpsJobList
    plural: opsjobs
    singular: opsjob
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              entryPoint:
                description: Opsjob entryPoint(startup command), required in base64,
                  only for preflight
                type: string
              env:
                additionalProperties:
                  type: string
                description: Environment variables
                type: object
              excludedNodes:
                description: The nodes to be excluded, for preflight/addon
                items:
                  type: string
                type: array
              hostpath:
                description: The hostpath for opsjob mounting.
                items:
                  type: string
                type: array
              image:
                description: Opsjob image address, only for preflight
                type: string
              inputs:
                description: |-
                  The resource objects to be processed, e.g., {{"name": "node", "value": "tus1-p8-g6"}}.
                  Multiple entries will be processed sequentially.
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              isTolerateAll:
                description: Indicates whether the job tolerates node taints. for
                  preflight, default false
                type: boolean
              resource:
                description: Opsjob resource requirements, only for preflight
                properties:
                  cpu:
                    description: Requested CPU core count (e.g., 128)
                    type: string
                  ephemeralStorage:
                    description: Ephemeral-storage for pod. default 50Gi
                    type: string
                  gpu:
                    description: Requested GPU card count (e.g., 8)
                    type: string
                  gpuName:
                    description: This field is set internally. e.g. amd.com/gpu
                    type: string
                  memory:
                    description: Requested Memory size (e.g., 128Gi)
                    type: string
                  rdmaResource:
                    description: |-
                      RDMA resource is effective only with hostNetwork enabled (default: 1).
                      This field is set internally
                    type: string
                  replica:
                    description: Number of requested nodes
                    type: integer
                  sharedMemory:
                    description: 'Requested Shared Memory size (e.g., 128Gi). Used
                      for sharing data between processes. default: Memory/2'
                    type: string
                required:
                - cpu
                - memory
                - replica
                type: object
              timeoutSecond:
                description: Opsjob Timeout (in seconds), Less than or equal to 0
                  means no timeout
                type: integer
              ttlSecondsAfterFinished:
                description: The lifecycle of ops-job after it finishes
                type: integer
              type:
                description: 'The type of ops-job, valid values include: addon/preflight/dumplog'
                type: string
            required:
            - inputs
            - isTolerateAll
            - type
            type: object
          status:
            properties:
              conditions:
                description: Description of the job execution process
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              finishedAt:
                description: Opsjob end time
                format: date-time
                type: string
              outputs:
                description: Opsjob output. For example, the download log URL or the
                  preflight check results.
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              phase:
                description: 'The job status: Succeeded/Failed/Running/Pending'
                type: string
              startedAt:
                description: Opsjob start time
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
