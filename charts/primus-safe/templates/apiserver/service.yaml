apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-apiserver
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: primus-safe-apiserver
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8088
  type: ClusterIP
