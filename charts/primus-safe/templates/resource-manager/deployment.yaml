apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-resource-manager
  namespace: {{ .Release.Namespace }}
  labels:
    app: primus-safe-resource-manager
spec:
  replicas: {{ .Values.resource_manager.replicas }}
  selector:
    matchLabels:
      app: primus-safe-resource-manager
  template:
    metadata:
      labels:
        app: primus-safe-resource-manager
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: feature.node.kubernetes.io/amd-gpu
                    operator: DoesNotExist
            - weight: 100
              preference:
                matchExpressions:
                  - key: feature.node.kubernetes.io/amd-gpu
                    operator: NotIn
                    values:
                      - "true"
      serviceAccountName: {{ .Release.Name }}-manager
      containers:
        - name: primus-safe-resource-manager
          image: {{ .Values.repository }}/resource-manager:{{ .Values.resource_manager.tag }}
          imagePullPolicy: Always
          command: ["/bin/sh", "/opt/primus-safe/resource-manager/run.sh"]
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          env:
            - name: crypto_file
              value: /etc/secrets/key
          volumeMounts:
            - name: primus-safe-secret
              mountPath: /etc/secrets
            - name: primus-safe-config
              mountPath: /opt/primus-safe/resource-manager/config
          ports:
            - containerPort: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 2
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 2
          resources:
            limits:
              cpu: "8000m"
              memory: 16Gi
            requests:
              cpu: "8000m"
              memory: 16Gi
      imagePullSecrets:
      - name: {{ .Release.Name }}-image
      volumes:
        - name: primus-safe-secret
          secret:
            secretName: {{ .Release.Name }}
        - name: primus-safe-config
          configMap:
            name: {{ .Release.Name }}-resource-manager
      terminationGracePeriodSeconds: 10
      priorityClassName: system-cluster-critical
      restartPolicy: Always
