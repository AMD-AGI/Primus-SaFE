apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: primus-safe-webhooks
  ports:
    - protocol: TCP
      port: 443
      targetPort: 8084
  type: ClusterIP
